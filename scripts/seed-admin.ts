/**
 * Placeholder for the admin user seeder.
 *
 * Phase 2 task list:
 *   1. Read ADMIN_DEFAULT_USER / ADMIN_DEFAULT_PASSWORD from env
 *   2. Connect to ADMIN_DB_URL via Drizzle
 *   3. Insert (idempotent) admin_users row with bcrypt-hashed password
 *   4. Print SUCCESS or already-exists; exit 0
 *
 * See docs/admin-integration.md §5 (bootstrap).
 */
throw new Error(
  'scripts/seed-admin.ts is a Phase 2 placeholder. ENABLE_ADMIN must stay 0 ' +
  'and admin seeder is not yet implemented. See docs/admin-integration.md.',
);